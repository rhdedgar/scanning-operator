apiVersion: v1
kind: Service
metadata:
  labels:
    control-plane: scanning-operator
  name: controller-manager-metrics-service
  namespace: openshift-scanning-operator
spec:
  ports:
  - name: https
    port: 8443
    targetPort: https
  selector:
    control-plane: scanning-operator
