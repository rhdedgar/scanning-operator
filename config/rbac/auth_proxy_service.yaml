apiVersion: v1
kind: Service
metadata:
  labels:
    control-plane: scanning-operator
  name: scanning-operator-metrics-service
  namespace: openshift-scanning-operator
spec:
  ports:
  - name: https
    port: 8443
    targetPort: https
  selector:
    control-plane: scanning-operator
